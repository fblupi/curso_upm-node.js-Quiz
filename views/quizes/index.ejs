<table>
<% var i; for (i=0; i < quizes.length; i++) { %>
  <tr>
    <td>[<%= quizes[i].tematica %>]</td>
    <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></td>
    <td><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
    <td>
      <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
        <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">borrar</button>
      </form>
    </td>
  </tr>
<% } %>
  <form method="get" action="/quizes">
    <label for="search">Palabra clave: </label>
    <input type="text" name="search" value="" placeholder="Escriba texto a buscar">
    <input type="submit" value="Enviar">
  </form>
</table><p>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>
